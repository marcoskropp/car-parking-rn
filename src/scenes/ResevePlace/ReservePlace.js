import React from 'react'
import { View, Text } from 'react-native'

import styles from '../../styles/global'

export const ReservePlace = () => (
  <View style={styles.container}>
    <Text>Reserve Place Screen</Text>
  </View>
)
