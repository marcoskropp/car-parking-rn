import React from 'react'
import { View, Text, Button } from 'react-native'

import styles from '../../styles/global'

export const UpdateVehicle = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Update Vehicle Screen</Text>
    </View>
  )
}
